const { execSync } = require('child_process');

try {
    const account = execSync('/flag').toString().trim();
    console.log(`${account}`);
} catch (error) {
    console.error('[-] 명령어 실행 실패:', error.message);
}
